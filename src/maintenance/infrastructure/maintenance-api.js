/**
 * Maintenance bounded-context API (IoT alerts / workshop monitoring).
 * Stub methods until the real backend is connected.
 */
export const maintenanceApi = {
    getAlerts() {
        return Promise.resolve({ data: [] });
    }
};
